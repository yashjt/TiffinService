<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tiffin Management - <%= title %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
            <a class="navbar-brand" href="/">Tiffin Management</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <a class="nav-link <%= (active || '') === 'home' ? 'active' : '' %>" href="/">Home</a>
                    <a class="nav-link <%= (active || '') === 'about' ? 'active' : '' %>" href="/about">About</a>

                    <a class="nav-link <%= (active || '') === 'contact' ? 'active' : '' %>" href="/contact">contact</a>

                    <% if (user) { %>
                        <li class="nav-item">
                            <a class="nav-link <%= active === 'order' ? 'active' : '' %>" href="/order">Order Food</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link <%= active === 'orders' ? 'active' : '' %>" href="/my-orders">My
                                Orders</a>
                        </li>
                        <% } %>
                </ul>
                <div class="navbar-nav">
                    <% if (!user) { %>
                        <a href="/login" class="nav-link <%= active === 'login' ? 'active' : '' %>">Login</a>
                        <a href="/register" class="nav-link <%= active === 'register' ? 'active' : '' %>">Register</a>
                        <% } else { %>
                            <span class="navbar-text me-3">Welcome, <%= user.username %></span>
                            <a href="/logout" class="nav-link">Logout</a>
                            <% } %>
                </div>
            </div>
        </div>
    </nav>